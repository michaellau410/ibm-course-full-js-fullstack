file 1
const express = require('express');
const multer = require('multer');
const path = require('path');
const fs = require('fs');

const app = express();
const port = 3000;

// Define the upload directory path
const directoryPath = 'uploads/';

// Set up storage for uploaded files
const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    cb(null, directoryPath); // Specify the upload directory
  },
  filename: function (req, file, cb) {
    cb(null, file.originalname); // Use the original file name
  },
});

const upload = multer({ storage: storage });

// Serve the HTML form for file upload
app.get('/', (req, res) => {
  res.sendFile(path.join(__dirname, 'index.html'));
});

// Handle file upload
app.post('/upload', upload.single('file'), (req, res) => {
  if (!req.file) {
    return next(new Error('No file uploaded.'));
  }

  // Access the uploaded file information
  const uploadedFile = req.file;
  console.log('Uploaded file:', uploadedFile);

  fs.readdir(directoryPath, (err,files)=>{
    if (err) {
      return res.status(500).send('Error reading directory.');
    }
    strfilenames = `<a href='/'>Home</a><br/>`;

    files.forEach((file)=>{
      strfilenames = `${strfilenames} <a target='_blank' href='file/${file}'>${file}</a><br/>`;
    });
    res.send(strfilenames)
  });

});

// Serve uploaded files using express.static middleware
app.use('/file', express.static('uploads'));

// Start the server
app.listen(port, () => {
  console.log(`Server is running on http://localhost:${port}`);
});

=======================
file 2
<!DOCTYPE html>
<html lang="en">
<body>
  <h1>File Upload</h1>
  <form action="/upload" method="post" enctype="multipart/form-data">
    <input type="file" name="file" />
    <input type="submit" value="Upload" />
  </form>
</body>
</html>

=======================
console log:

theia@theia-michaellau41:/home/project$ ls
theia@theia-michaellau41:/home/project$ mkdir fileuploadapp
theia@theia-michaellau41:/home/project$ cd fileuploadapp
theia@theia-michaellau41:/home/project/fileuploadapp$ npm init -y
Wrote to /home/project/fileuploadapp/package.json:

{
  "name": "fileuploadapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}



theia@theia-michaellau41:/home/project/fileuploadapp$ npm install express multer

added 83 packages, and audited 84 packages in 2s

29 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
theia@theia-michaellau41:/home/project/fileuploadapp$ touch fileuploadapp.js
theia@theia-michaellau41:/home/project/fileuploadapp$ touch index.html
theia@theia-michaellau41:/home/project/fileuploadapp$ mkdir uploads
theia@theia-michaellau41:/home/project/fileuploadapp$ node fileuploadapp.js
Server is running on http://localhost:3000
Uploaded file: {
  fieldname: 'file',
  originalname: 'Screenshot 2026-09-03 174502.png',
  encoding: '7bit',
  mimetype: 'image/png',
  path: 'uploads/Screenshot 2026-09-03 174502.png',
  destination: 'uploads/',
  filename: 'Screenshot 2026-09-03 174502.png',
  size: 2906898
}
^C
theia@theia-michaellau41:/home/project/fileuploadapp$ 
